window.app = window.app || {};
window.data = app.data = app.data || {};
window.modules = app.module = app.module || {};
window.gapi = app.module.googleapi = app.module.googleapi || {};
window.view = app.view = app.view || {};
